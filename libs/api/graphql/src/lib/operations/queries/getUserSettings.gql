query getSettings($userId: ID!) {
  getSettings(userId: $userId) {
    __typename

    defaultCurrency
    defaultExchange {
      __typename

      abbreviation
      suffix
      mic
      name
      region
    }
  }
}
