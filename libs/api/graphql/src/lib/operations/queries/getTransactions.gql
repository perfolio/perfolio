query getTransactions($userId: ID!) {
  getTransactions(userId: $userId) {
    id
    asset {
      ... on Stock {
        id
        ticker
      }
      ... on Crypto {
        id
      }
    }
    executedAt
    value
    volume
  }
}
