query user($userId: ID!) {
  user(userId: $userId) {
    __typename
    id
    stripeCustomerId
    settings {
      __typename

      defaultCurrency
      defaultExchange {
        __typename
        abbreviation
        suffix
        mic
        name
        region
      }
    }
  }
}
